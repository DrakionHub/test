import discord

client = discord.Client(intents=discord.Intents.default())

@client.event
async def on_ready():
    print("Bot online!")
import os

TOKEN = os.getenv("TOKEN")
bot.run(TOKEN)
